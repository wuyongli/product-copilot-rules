---
name: product-copilot-rules
description: >
  当产品目标、业务语义、范围取舍、验收标准或数据与上线策略尚未明确时使用；
  不用于目标清晰的纯技术实现、明确技术 Bug 或无产品决策的局部重构。
---

# 产品方案协作规范

帮助用户把想法收敛成建立在当前系统之上的可执行方案。AI 负责提出判断和推荐路径，用户确认业务方向、边界、关键取舍与开工授权。

## 适用边界

先按请求本质分流：

| 场景 | 处理方式 |
| --- | --- |
| 产品目标、业务语义、范围或验收不清 | 使用本 skill |
| 目标明确，只需归纳边界或开发落点 | 使用轻量快速路径 |
| 明确技术 Bug | 直接诊断和最小修复，不进入完整产品流程 |
| 业务规则 Bug | 精简确认业务口径、影响范围和验收标准 |
| 止血与长期治理存在取舍 | 使用完整模式 |
| 目标明确的纯技术实现或局部重构 | 不使用本 skill |

用户说“只做需求分析”时停在业务分析和初步建议；用户说“先分析”或“先不要改代码”时可以做必要的只读系统取证，但不进入实现。用户说“先不要读代码”时不执行系统取证，只标记仍待验证的系统假设。

## 协作原则

- 先判断为什么做、解决什么问题、成功标准是什么，再讨论实现方式。
- 不把用户提出的手段直接当作真实需求；明确关键矛盾、收益和代价。
- 先给 AI 推荐方案和理由，再提出会改变范围或取舍的必要问题。
- 区分“已确认事实”“AI 建议”“待确认事项”，未经确认的建议不能伪装成结论。
- 方案必须基于当前系统能力与约束；优先复用或局部改造，再考虑新增能力。
- 不为模板完整度扩张需求；条件内容不适用时直接省略。

## 与 `task-workflow` 配合

- `task-workflow` 管理任务生命周期、仓库绑定、阶段状态、开发准入、文档路径和恢复流程，也可以创建最小种子版 `plan.md`。
- `product-copilot-rules` 管理方案分析方法、内容要求和正式方案展开方式。
- 已有 `plan.md` 时原地归并和重写同义章节，不创建第二份主方案，也不要求初始骨架与本 skill 模板逐字一致。
- `plan.md` 保留当前有效结论；候选方案和改口过程写入 `decision-log.md`；执行、验证、发布和阻塞写入 `progress.md`；机器状态以 `meta.yaml` 为准。
- 文档结构选择和按需模板见 [references/plan-template.md](references/plan-template.md)。

## 选择执行深度

### 轻量快速路径

同时满足以下条件时使用：目标、范围、约束、验收标准以及系统落点或目标仓库已经明确，且不存在会改变方案的关键业务假设。

一次完成：

1. 简短需求理解和范围确认。
2. 与请求直接相关的定向代码取证。
3. 紧凑的推荐方案、开发落点和验证重点。
4. 只列仍会改变范围、取舍或上线方式的待确认项。

不机械输出 Stage 标题，不重复索要确认。用户只要求需求分析时省略开发落点；按上述四项紧凑输出即可，不再加载完整模式模板。

### 完整模式

目标、对象、成功标准不清，或存在跨系统流程、权限、数据、上线策略和明显范围争议时使用。

正式顺序为：`Stage 0 → Stage 1 → Stage 2 → 用户确认 → Stage 3 → Stage 4 → Stage 4.5 → Stage 5`。详细判断、输出和停止条件见 [references/full-workflow.md](references/full-workflow.md)。

关键确认边界：

- Stage 2 只要求用户确认业务目标、核心流程、范围、关键取舍和验收口径。
- Stage 2 的系统影响、数据变化和上线方式只能作为初步判断，不要求用户确认技术机制。
- Stage 3 完成正式系统取证后再形成系统、数据和上线结论。
- 如果 Stage 3 改变了已确认的业务范围或关键取舍，只确认变化部分；不要让用户重复确认未变化内容。

### 停止出口

需求不建议直接做、应先验证、关键业务事实不足、主改仓不明或当前系统约束不支持时，停在分析或初步建议：说明当前判断、停止原因和最小下一步，不强行生成完整方案。

## 系统取证

- 先确认当前仓库是否承载主要业务逻辑；多仓任务先识别主改仓和配套仓。
- 优先读取相关路由、接口、服务、模型、权限、配置、测试和相似实现，不泛读整个仓库。
- 把证据写入“已有能力、主要约束、可复用部分和推荐路径”，不要只在推理中使用。
- 主改仓仍不明确时停止深入取证，先报告仓库判断和缺失信息。
- 涉及表、字段、索引或历史数据时，读取 [references/data-change-plan.md](references/data-change-plan.md)，不要默认新表自动创建或默认需要审批 DDL。

## 方案文档

- 默认先写种子版；业务方向、范围、系统落点和关键取舍稳定后再扩为正式版。
- 固定核心只包含目标与成功标准、当前系统现状、范围与取舍、推荐方案、验收、风险与待确认项。
- 角色流程、非功能要求、依赖、开发、数据、权限、灰度、监控、迁移、上线和回滚均为条件章节。
- 当前阶段不推进的模块只保留边界或导航；详细设计可拆到模块子方案，远期信息可拆到附录。
- 稳定结论与核心原因必须回写 `plan.md`，不要在文末并列追加新旧口径。

## 开发方案与代码准入

当用户询问如何开发或准备进入实现时，先给出按业务改动点或依赖阶段组织的开发方案。只有确有公共前置改造时才单列基础层。

开始代码工作必须同时满足：

1. 产品方案已经确认。
2. 开发方案已补齐到可执行粒度。
3. 用户明确允许开始代码工作。

编码时严格对齐已确认范围。发现关键方案缺口时暂停实现，回到对应确认点；不要借实现过程扩张需求。

## 输出与自检

- 输出推荐结论和依据，不只罗列信息或问题。
- 标记未确认假设、边界、风险和下一步。
- 涉及的条件章节必须可执行、可验证；不涉及的章节直接省略。
- 方案输出后检查需求成立性、范围、验收、系统证据、数据与上线影响、风险和开工条件。
- 没有发现风险时简短说明，不重复打印完整检查清单。
